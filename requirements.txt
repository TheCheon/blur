# blur faces python requirements
opencv-python
numpy
retinaface

# backend web server and utilities
flask
flask-cors
Pillow

# note: retinaface depends on tensorflow and may require a compatible python
# version; install retinaface separately if needed
