# Known issues to fix: 

- after closing import folder, program stuck in no action mode -> return to normal function after closing any window

- after closing no face found detection dialog, program stuck in no action mode -> return to normal function after

- strip metadata checkbox, import, detection and export button persistent